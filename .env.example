# Leave unset for local dev (Vite proxies /api and /uploads to localhost:5000).
# For split hosting (e.g. Vercel frontend + Render backend), set this to the
# backend's full public URL, e.g.:
# VITE_API_BASE_URL=https://your-backend.onrender.com/api/v1
VITE_API_BASE_URL=
